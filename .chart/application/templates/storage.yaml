apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.web.name }}-pvc
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi